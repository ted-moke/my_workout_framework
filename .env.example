# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=workout
DB_PASSWORD=workout
DB_NAME=workout_tracker

# Server Configuration
PORT=3001
NODE_ENV=production

# Cloudflare Tunnel
# Create a tunnel at https://one.dash.cloudflare.com → Networks → Tunnels
# Configure public hostname to point to http://app:3001
TUNNEL_TOKEN=
